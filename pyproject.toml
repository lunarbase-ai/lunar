[tool.poetry]
name = "lunar"
version = "0.1.0"
description = ""
authors = ["Lunarbase Labs <contact@lunarbase.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
